标记分为标记接口和标记注解。
标记接口：没有包含方法声明的接口，只是指明某个类实现了具有某种属性的接口。比如Serializable接口。
标记接口与标记注解的最终要的区别在于：标记接口可以在编译时就检查到相应的类型问题，而标记注解则要到运行时。
当标记要应用到任何程序元素，包括方法、域等，而不仅仅是接口和类，或在未来会给标注添加更多信息，或要适应已经广泛使用注解类型的框架，那么应该使用标记注解；
当标记只应用给类和接口，定义一个任何新方法都不会与之关联的标记类型，就应该使用标记接口。
